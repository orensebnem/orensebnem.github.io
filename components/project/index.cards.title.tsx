import React from "react";
import {Text} from "@chakra-ui/react";

const SProjectCardsTitle = () => {
    return (
        <Text fontSize={["md", "2xl", "3xl", "5xl"]}
              ml={4}
              mb={2}
              textAlign="center"
              fontFamily={"primary"}
              fontWeight={900}
              mt={10}>PORTFOLIO</Text>
    )
}

export default SProjectCardsTitle;
